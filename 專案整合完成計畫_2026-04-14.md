# WoundAI 專案整合完成計畫 — 全平台深度分析與實施路線圖

**分析日期**：2026-04-14
**上次架構規劃**：2026-03-26
**當前階段**：Phase 1 末段 → Phase 2 過渡期

---

## 一、Sprint A–E 計畫執行驗證

| Sprint | 計畫內容 | Git 驗證 | 狀態 |
|--------|---------|---------|------|
| A | iOS TODO 修正 + Flask JWT 認證 | `908147c` — ContentView、LiDARCalibration 修正、Flask JWT | ✅ 已完成 |
| B | FastAPI 端點補全 + Windows CloudAPI | `cb2d41f` — 7 個 FastAPI endpoint + CloudAPIService.cs | ✅ 已完成 |
| C | Android TFLite 優化 + 多模型集成 | `61c3769` — MultiModelClassifier、DepthEstimation、FP16 量化腳本 | ✅ 已完成 |
| D+E | Docker 部署 + OpenAPI 規格 | `04ed460` — Dockerfile×2、docker-compose、nginx、woundai-api.yaml | ✅ 已完成 |
| 追加修復 | 批次/PDF/標注 + ModelManager 缺失 | `5a429f8` `12238e3` — Android/Windows 批次+PDF+標注、ML __init__ | ✅ 已完成 |

**結論**：原計畫 Sprint A–E 全數已提交，但部分功能為框架/Stub 實作，尚未接入真實 ML 模型。

---

## 二、各平台當前真實完成度（深度代碼審計結果）

### 2.1 iOS — 完成度 95%（參考實作）

| 模組分類 | 檔案數 | 狀態 | 備註 |
|---------|--------|------|------|
| Core Models | 7 | ✅ 完整 | AppStateManager、DataManager、WoundRecord、WoundTypes、SharedTypes |
| Capture/Camera | 1 | ✅ 完整 | AVCaptureSession、LiDAR hooks、CoreData 整合 |
| Calibration | 5 | ✅ 完整 | Sticker、Square、LiDAR、Ruler、Color 校正 |
| Segmentation | 4 | ✅ 完整 | CoreML UNet256、UWM Lightweight、MultiScale、Adaptive |
| Classification | 3 | ✅ 完整 | Advanced ML、SimplifiedML、Multi-algorithm |
| Cloud Integration | 5 | ✅ 完整 | CloudAPI、LocalCloud sync、Comparator |
| Quality/Validation | 5 | ✅ 完整 | Medical Grade Validator (FDA/CE ready) |
| Views (SwiftUI) | 20+ | ✅ 完整 | 全部完成，含 AR、3D、Batch、Cloud 視圖 |
| Services | 7 | ✅ 完整 | PDF 報告、API、Performance、BatchProcessing |
| Tests | 6 | ✅ 框架 | 測試基礎設施完備 |

**剩餘問題（3 項低優先級）**：
1. `ContentView.swift:5474` — `// TODO: 實現保存邏輯`（CoreData save flow 待接通）
2. `StickerDetector.swift` — 僅 42 行，最小化 stub
3. `CloudAPIService.swift` — Base URL 為 placeholder (`woundai-api.example.com`)

---

### 2.2 Android — 完成度 80%

| 模組分類 | 狀態 | 詳情 |
|---------|------|------|
| Camera + Quality | ✅ 完整 | AdvancedCameraModule (Camera2)、ImageQualityAssessor (7維) |
| MultiModelClassifier | ✅ 完整 | 3-model TFLite ensemble, softmax, fallback 機制 |
| DepthEstimation | ✅ 完整 | ARCore / MiDaS / fallback 三層策略 |
| SmartROI | ✅ 完整 | OpenCV 4 階段 ROI 偵測 |
| Patient + DB | ✅ 完整 | Room DAO、Repository、Entity 全套 |
| AnalysisHistory | ✅ 完整 | 線性回歸趨勢分析（但使用 mock 歷史資料） |
| BatchProcessing | ✅ 完整 | 批次影像處理 pipeline |
| PDFReport | ✅ 完整 | A4 Android PdfDocument API |
| Annotation (BJWAT/revPWAT) | ✅ 完整 | 雙標準評分系統 |
| 3D Visualization | ⚠️ 骨架 | OpenGL ES 2.0 框架，渲染不完整 |
| **RulerCalibration** | ❌ **Stub** | OpenCV 完全註解掉，返回 mock (scale=10.0) |
| **OpenCVProcessor** | ❌ **Stub** | 輪廓分析未實作，返回固定值 (area=25.0) |
| **RealTimeAnalysis engines** | ❌ **Stub** | SegmentationEngine → always true, MeasurementEngine → fixed values |
| **DoctorAuth** | ⚠️ 硬編碼 | 3 組測試帳密，未接後端 |
| **CloudServiceConnector** | ⚠️ Placeholder URL | `https://your-cloud-service.com/api` |
| **MainActivity** | ⚠️ 不完整 | History/Settings 畫面為 loading placeholder |

**關鍵阻斷問題**：
- RulerCalibration + OpenCVProcessor 均為 stub → **任何實際量測結果都不準確**
- RealTimeAnalysis 內部引擎返回硬編碼值 → 即時分析功能無效

---

### 2.3 Windows — 完成度 70%

| 模組分類 | 狀態 | 詳情 |
|---------|------|------|
| Core Interfaces (4) | ✅ 完整 | IAIModule、ICaptureModule、IMeasurementModule、IProcessingModule |
| ImageData Models | ✅ 完整 | 完整資料模型含 depth/confidence |
| WoundMeasurementSystem | ✅ 完整 | 主 orchestrator 全流程整合 |
| OpenCVCapture | ✅ 完整 | 6 因子品質評估、Camera2 |
| DepthCapture (RealSense) | ✅ 完整 | Intel RealSense SDK |
| RulerCalibration | ✅ 完整 | Grid + 色彩角落偵測 |
| CloudAPIService | ✅ 完整 | JWT auto-refresh、multipart upload |
| BatchProcessing | ✅ 完整 | 並行處理 + 進度回報 |
| PDFReport | ✅ 完整 | HTML 格式報告 |
| DoctorAnnotation | ✅ 完整 | CRUD + 審核 workflow |
| 3D Visualization | ✅ 完整 | OBJ 匯出、mesh 生成 |
| MainViewModel (WPF) | ✅ 完整 | MVVM Community Toolkit |
| **MockProcessingModule** | ❌ **Stub** | 返回固定 quality=80.0 |
| **MockAIModule** | ❌ **Stub** | 返回固定 60% Acute |
| **MockMeasurementModule** | ❌ **Stub** | 返回固定值 (150.5 mm², 45.2 mm) |
| **RealTimeAnalysis engines** | ❌ **Stub** | DetectWound=true, area=10.0, volume=1.0 |
| **AI/ directory** | ❌ **空** | 無 AI 模組實作 |
| **Processing/ directory** | ❌ **空** | 無影像處理實作 |
| **Measurement/ directory** | ❌ **空** | 無量測演算法 |
| **Data/ directory** | ❌ **空** | 無資料存取層 |
| **Tests/ directory** | ❌ **空** | 無測試 |

**關鍵阻斷問題**：
- 三個 Mock Module → 整個量測 pipeline 產出都是假資料
- 四個空目錄需要實際實作才能使 Windows 版本可用

---

### 2.4 Backend Flask — 完成度 80%

| 功能 | 狀態 | 詳情 |
|------|------|------|
| REST API (7 endpoints) | ✅ 完整 | analyze, batch, train, retrain, health, auth, volume |
| JWT Authentication | ✅ 完整 | Sprint A 加入 |
| Dual-model Segmentation | ✅ 框架 | UWM + Deepskin（需實際模型檔） |
| Image Quality + Processing | ✅ 完整 | 512x512 resize、HSV fallback |
| Tissue Classification | ✅ 完整 | 色彩分析 granulation/necrotic/fibrin |
| Depth/Volume Calculation | ✅ 完整 | ARKit 格式支援 |
| Docker + Gunicorn + Nginx | ✅ 完整 | Sprint D 加入 |
| **Model Retraining** | ❌ **Placeholder** | `retrain_models_async()` 只 sleep 60 秒 |
| **uwm_deepskin_service.py** | ⚠️ 獨立 | 與 main app.py 是兩個獨立 Flask app |
| **模型檔案** | ❌ **缺失** | uwm_mobilenetv2.h5、deepskin model 不存在 |
| **ImageJ Headless** | ⚠️ 未使用 | import 但未啟用 |

---

### 2.5 Cloud FastAPI — 完成度 72%

| 功能 | 狀態 | 詳情 |
|------|------|------|
| API Endpoints (7) | ✅ 完整 | auth, upload, quality, analyze, status, training/jobs, training/enqueue |
| JWT Authentication | ✅ 完整 | OAuth2PasswordBearer |
| Quality Assessment | ✅ 完整 | Laplacian blur、brightness、contrast、SNR |
| ModelManager pipeline | ✅ 框架 | 整合 3 個 ML model class |
| Celery + Redis Worker | ✅ 完整 | 訓練任務佇列 |
| Docker + Uvicorn | ✅ 完整 | Sprint D |
| **WoundSegmentation** | ❌ **Mock** | `np.random.rand(224, 224)` 隨機 mask |
| **BJWATClassifier** | ❌ **Mock** | 返回硬編碼 11 維分數 |
| **RevPWATClassifier** | ❌ **Mock** | 返回硬編碼 6 維分數 |
| **Training Task** | ⚠️ **Simulated** | 模擬 0.05s/batch，假 metrics |
| **Alembic Migrations** | ❌ **缺失** | 無 migration 版本檔案 |
| **Email Service** | ⚠️ Placeholder | mail.py 未接入端點 |

---

### 2.6 Deployment — 完成度 85%

| 組件 | 狀態 | 詳情 |
|------|------|------|
| docker-compose.yml (5 services) | ✅ 完整 | nginx、flask、fastapi、redis、worker |
| Flask Dockerfile | ✅ 完整 | Multi-stage、non-root user |
| FastAPI Dockerfile | ✅ 完整 | Multi-stage、non-root user |
| Nginx Config | ✅ 完整 | Rate limiting、proxy、security headers |
| OpenAPI Spec | ✅ 完整 | woundai-api.yaml (OpenAPI 3.1) |
| SSL Certificate | ⚠️ 腳本 | generate-ssl.sh（自簽憑證） |
| **.env 檔案** | ❌ 缺失 | 只有 .env.example |
| **CORS 設定** | ⚠️ 不安全 | FastAPI CORS allow all origins (*) |
| **Hardcoded 預設密碼** | ⚠️ 安全風險 | admin/woundai-admin 寫死在 config.py |

---

## 三、跨平台功能對照矩陣（2026-04-14 真實狀態）

| 功能 | iOS | Android | Windows | Flask | FastAPI | 整合狀態 |
|------|:---:|:-------:|:-------:|:-----:|:-------:|:--------:|
| 影像擷取 | ✅ | ✅ | ✅ | - | - | ✅ 完成 |
| 影像品質評估 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ 完成 |
| 校正（尺規/貼紙） | ✅ | ❌ stub | ✅ | - | - | ⚠️ Android |
| LiDAR 深度 | ✅ | N/A | ✅ RealSense | - | - | ✅ 完成 |
| 軟體深度估算 | ✅ | ✅ MiDaS | - | - | - | ⚠️ Win |
| SmartROI | ✅ | ✅ | ⚠️ 部分 | - | - | ⚠️ Win |
| AI 分割 | ✅ CoreML | ✅ TFLite | ❌ mock | ⚠️ 需模型 | ❌ random | 🔴 **關鍵缺口** |
| 傷口分類 | ✅ | ✅ ensemble | ❌ mock | ✅ HSV | ❌ hardcoded | 🔴 **關鍵缺口** |
| 面積/周長量測 | ✅ | ❌ stub | ❌ mock | ✅ | ✅ 框架 | 🔴 **關鍵缺口** |
| 體積計算 | ✅ | ✅ | ✅ | ✅ | ✅ 框架 | ⚠️ 精度 |
| 3D 視覺化 | ✅ SceneKit | ⚠️ 骨架 | ✅ | - | - | ⚠️ Android |
| BJWAT/revPWAT 標注 | ✅ | ✅ | ✅ | - | ❌ mock | ⚠️ 後端 |
| 即時分析 | ✅ | ❌ stub engine | ❌ stub engine | - | - | 🔴 **關鍵缺口** |
| 批次處理 | ✅ | ✅ | ✅ | ✅ | - | ✅ 完成 |
| PDF 報告 | ✅ | ✅ | ✅ HTML | - | - | ✅ 完成 |
| Cloud API 連線 | ⚠️ URL | ⚠️ URL | ✅ | ✅ | ✅ | ⚠️ URL 設定 |
| JWT 認證 | ⚠️ | ⚠️ | ✅ | ✅ | ✅ | ⚠️ 客戶端 |
| 歷史趨勢分析 | ✅ | ✅ mock data | ⚠️ 基礎 | - | - | ⚠️ |
| Docker 部署 | - | - | - | ✅ | ✅ | ✅ 完成 |
| OpenAPI 規格 | - | - | - | ✅ | ✅ | ✅ 完成 |

---

## 四、關鍵阻斷問題彙整（必須解決才能上線）

### 🔴 P0 — 系統無法產出有效結果

| # | 問題 | 影響範圍 | 根因 |
|---|------|---------|------|
| 1 | **Cloud ML 模型全為 Mock** | FastAPI 所有分析 | wound_segmentation 返回 random, classifiers 返回 hardcoded |
| 2 | **Android RulerCalibration = stub** | Android 所有量測 | OpenCV 整段註解掉 |
| 3 | **Android OpenCVProcessor = stub** | Android 輪廓分析 | 返回固定 area=25.0 |
| 4 | **Windows 三個 Mock Module** | Windows 全部功能 | MockProcessing/AI/Measurement |
| 5 | **Android RealTimeAnalysis engine = stub** | Android 即時分析 | DetectWound=true, area=10.0 |
| 6 | **Windows RealTimeAnalysis engine = stub** | Windows 即時分析 | 同上 |
| 7 | **Flask 缺實際模型檔案** | Flask 分割推論 | .h5/.pb 檔案未提供 |

### 🟡 P1 — 功能缺失但不阻斷

| # | 問題 | 影響範圍 |
|---|------|---------|
| 8 | Cloud API URL 為 placeholder (iOS/Android) | 客戶端無法連雲端 |
| 9 | Hardcoded 認證憑證 (Flask/FastAPI/Android) | 安全性風險 |
| 10 | CORS allow all (*) | 生產安全性 |
| 11 | Alembic migration 檔案缺失 | DB schema 管理 |
| 12 | iOS ContentView save logic TODO | 量測結果未持久化 |
| 13 | Flask retrain = sleep placeholder | 無法真正重訓模型 |
| 14 | Android 3D Visualization 渲染不完整 | 3D 視覺品質 |

---

## 五、Phase 1.5 完成計畫 — 實施路線圖

### Sprint F（第 1-2 週）：解決 P0 阻斷 — ML 模型實體化

**目標**：讓所有平台的 AI 推論產出真實結果

#### F1. Cloud FastAPI ML 模型替換（2-3 天）
```
檔案：雲端 AI 模型訓練及分析服務/app/ml/models/
- wound_segmentation.py → 載入真實 UNet/FUSegNet 權重 (.pth/.h5)
- bjwat_classifier.py → 載入訓練好的 BJWAT 評分模型
- revpwat_classifier.py → 載入訓練好的 revPWAT 評分模型

前置條件：需確認 weights/ 目錄下可用的模型權重
來源：weights/restored_onedrive_20251002-171321/
外部：FUSegNet-main/、Deepskin-main/、wound-segmentation-master/
```

#### F2. Flask 模型檔案掛載（1-2 天）
```
檔案：Backend/Flask/app.py
- 確認 UWM MobileNetV2 模型路徑與載入
- 確認 Deepskin 模型路徑與載入
- 移除 dummy U-Net fallback（或保留為明確 error）
- 整合 uwm_deepskin_service.py 至 main app（消除雙 Flask app）
```

#### F3. Android OpenCV 啟用（2-3 天）
```
檔案：
- RulerCalibrationModule.kt → 恢復 OpenCV 校正邏輯
  - Canny edge detection
  - Hough line detection
  - Scale calculation
- OpenCVProcessor.kt → 實作傷口輪廓分析
  - BGR→HSV 轉換
  - 色彩閾值分割
  - findContours → contourArea/arcLength
- RealTimeAnalysisModule.kt → 接入 MultiModelClassifier
  - SegmentationEngine → 呼叫 TFLite segmentation
  - MeasurementEngine → 呼叫 OpenCVProcessor
```

#### F4. Windows Mock Module 替換（3-4 天）
```
檔案：
- Windows/Processing/OpenCVProcessingModule.cs（新建）
  → 實作 IProcessingModule：白平衡、雜訊降低、ROI 偵測
- Windows/AI/ONNXAIModule.cs（新建）
  → 實作 IAIModule：ONNX Runtime 載入 wound_classification.onnx
- Windows/Measurement/WoundMeasurementModule.cs（新建）
  → 實作 IMeasurementModule：OpenCvSharp 輪廓分析
- Windows/WPF/App.xaml.cs → 替換 DI 註冊
  → MockProcessingModule → OpenCVProcessingModule
  → MockAIModule → ONNXAIModule
  → MockMeasurementModule → WoundMeasurementModule
- Windows/Core/Services/RealTimeAnalysisService.cs
  → SegmentationEngine/MeasurementEngine 接入真實模組
```

---

### Sprint G（第 3-4 週）：P1 修復 + 跨平台聯調

#### G1. Cloud API URL 統一配置（1 天）
```
所有平台 API 客戶端 base URL 設為可配置：
- iOS:     CloudAPIService.swift → 讀取 Info.plist 或 UserDefaults
- Android: CloudServiceConnector.kt → 讀取 BuildConfig 或 SharedPreferences
- Windows: CloudAPIService.cs → 讀取 appsettings.json
統一指向：部署後的 docker-compose 服務 URL
```

#### G2. 認證安全化（2 天）
```
- FastAPI config.py → 移除 hardcoded credentials，改讀環境變數
- Flask app.py → JWT secret 改讀環境變數
- Android DoctorAuthActivity → 接入 Cloud auth API
- 建立 .env 檔案（從 .env.example）
- CORS 設定改為白名單模式
```

#### G3. iOS 剩餘 TODO（1 天）
```
- ContentView.swift:5474 → 實作 DataManager.saveWoundRecord()
- StickerDetector.swift → 完善或移除（已有 CalibrationStickerModule）
```

#### G4. Alembic Migration 初始化（0.5 天）
```
cd "雲端 AI 模型訓練及分析服務"
alembic init alembic  (如尚未 init)
alembic revision --autogenerate -m "initial schema"
alembic upgrade head
```

#### G5. 端對端聯調測試（3 天）
```
測試矩陣：
1. iOS → FastAPI /api/v1/analyze → 驗證真實分析結果
2. Android → FastAPI /api/v1/analyze → 驗證真實分析結果
3. Windows → FastAPI /api/v1/analyze → 驗證真實分析結果
4. iOS → Flask /api/analyze → 驗證雙模型共識
5. Batch upload → training/enqueue → worker 執行
6. JWT 認證流程 → token refresh → protected endpoints
```

---

### Sprint H（第 5-6 週）：品質提升 + 準確度驗證

#### H1. 模型準確度基準測試（3 天）
```
使用 test_images/ 目錄下的真實影像：
- test_images/Bedsores/
- test_images/Body_Chronic_Wound/
- test_images/Leg_Chronic_Wound/

指標：
- Segmentation: Dice coefficient ≥ 0.85, IoU ≥ 0.80
- Classification: Accuracy ≥ 90%, F1 ≥ 0.85
- Area measurement: ±5% vs ground truth
- Volume estimation: ±15% vs reference
```

#### H2. Android 3D Visualization 完善（2 天）
```
- Wound3DVisualizationModule.kt → 完善 OpenGL ES 渲染
- Vertex coloring 深度梯度
- Touch interaction (rotation/zoom)
```

#### H3. Windows Data Layer 實作（2 天）
```
- Windows/Data/ → 實作 SQLite 資料存取層
  - WoundRecordEntity
  - PatientEntity
  - DAOs with Dapper or EF Core
- 歷史記錄查詢功能
```

#### H4. Windows Tests（2 天）
```
- Windows/Tests/ → 單元測試
  - OpenCVProcessingModuleTests
  - ONNXAIModuleTests
  - WoundMeasurementModuleTests
  - CloudAPIServiceTests
  - Integration pipeline test
```

---

### Sprint I（第 7-8 週）：進階功能 + 部署準備

#### I1. Flask 模型重訓實作（3 天）
```
- 替換 retrain_models_async() placeholder
- 實作 incremental fine-tuning pipeline
- 接入 training_data table 的 ground truth
- 輸出新模型版本至 models/ 目錄
```

#### I2. 模型版本管理（2 天）
```
- FastAPI 加入 model_version 追蹤
- API response 包含 model_version 欄位
- 支援多模型版本共存
- A/B testing 框架
```

#### I3. 生產部署 checklist（2 天）
```
□ .env 生產設定完成
□ SSL 憑證（非自簽）
□ CORS 白名單設定
□ Rate limiting 調整
□ 監控 + logging（Grafana/Prometheus）
□ Backup 策略
□ CI/CD pipeline（GitHub Actions）
```

#### I4. Email 通知整合（1 天）
```
- FastAPI mail.py → 接入 SMTP 或 SendGrid
- 觸發條件：訓練完成、異常傷口偵測、系統錯誤
```

---

## 六、Phase 2 預備規劃（Sprint I 完成後）

| 階段 | 目標 | 預估時間 |
|------|------|---------|
| Phase 2A | FHIR R4 資源對應（Patient→Observation→DiagnosticReport） | 4 週 |
| Phase 2B | EHR 系統介接（HL7 gateway） | 4 週 |
| Phase 2C | 臨床決策支援（CDS Hooks — 敷料建議、惡化警示） | 3 週 |
| Phase 2D | 多中心臨床驗證試驗準備 | 6 週 |
| Phase 3 | FDA 510(k) / CE MDR 法規文件準備 | 12 週 |

---

## 七、優先級排序總覽

```
緊急度 ────────────────────────────────────────────────►

  Sprint F (Week 1-2)     Sprint G (Week 3-4)     Sprint H (Week 5-6)     Sprint I (Week 7-8)
  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
  │ 🔴 P0 阻斷修復   │    │ 🟡 P1 修復+聯調  │    │ 品質+準確度驗證   │    │ 進階功能+部署    │
  │                 │    │                 │    │                 │    │                 │
  │ F1 Cloud ML 模型 │    │ G1 API URL 配置  │    │ H1 準確度基準    │    │ I1 模型重訓實作  │
  │ F2 Flask 模型掛載 │    │ G2 認證安全化    │    │ H2 Android 3D   │    │ I2 版本管理     │
  │ F3 Android OpenCV│    │ G3 iOS TODO     │    │ H3 Win Data層   │    │ I3 部署checklist │
  │ F4 Windows Mocks │    │ G4 Alembic      │    │ H4 Win Tests    │    │ I4 Email 通知    │
  │                 │    │ G5 端對端聯調    │    │                 │    │                 │
  └─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
        ↓                      ↓                      ↓                      ↓
  平台可產出真實結果      全平台互聯互通         醫療級準確度驗證        生產就緒 → Phase 2
```

---

## 八、風險與前置條件

| 風險 | 影響 | 緩解措施 |
|------|------|---------|
| **模型權重不可用** | Sprint F 全部延遲 | 確認 weights/ 目錄，必要時重新訓練 |
| **OpenCV Android 版本衝突** | F3 延遲 | 確認 build.gradle OpenCV 4.8.0 相容性 |
| **ONNX Runtime Windows 配置** | F4 延遲 | 先用 ONNX Runtime NuGet 包測試 |
| **GPU 不可用（Cloud）** | 推論速度慢 | CPU 推論 + 批次佇列 |
| **測試影像不足** | H1 基準不準確 | 從 批次驗證工具/ 補充影像 |

---

## 九、建議立即行動

1. **確認模型權重狀態**：檢查 `weights/restored_onedrive_20251002-171321/` 內容，確認哪些 .h5/.pth/.tflite 檔案可直接使用
2. **從 Sprint F1 開始**：Cloud FastAPI ML 模型替換是最高槓桿任務，解決後所有平台的雲端分析都能產出真實結果
3. **F3 Android OpenCV 與 F4 Windows Mocks 可並行**：兩個平台的本地推論修復互不依賴

---

*本文件基於 2026-04-14 完整代碼審計結果撰寫，取代先前 2026-03-26 版本的架構規劃。*
*代碼審計範圍：iOS 96 檔案、Android 36 檔案、Windows 19 檔案、Backend 3 檔案、Cloud 26 檔案、Deployment 5 檔案。*
