# WoundAI Windows test environment.
# Keep this aligned with .github/workflows/phase0-check.yml and
# Backend/Flask/requirements.txt.  Training-only GPU packages are deliberately
# excluded; those belong to the private WoundAI training environment.

numpy>=1.26,<3
onnxruntime>=1.17
Pillow>=10
PyYAML>=6
jsonschema>=4

Flask>=3
flask-cors>=4
flask-jwt-extended>=4.6
Werkzeug>=3
requests>=2.31
google-cloud-storage>=2.14

fastapi>=0.111
pydantic>=2.7
python-multipart>=0.0.9
httpx>=0.27,<0.28

# ArUco is used by calibration tests.  Use one OpenCV wheel only; installing
# opencv-python-headless beside this package can make cv2 resolution ambiguous.
opencv-contrib-python-headless>=4.9

pytest>=8
pytest-asyncio>=0.23
coverage>=7
