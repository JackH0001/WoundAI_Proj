# gcloud run deploy --source . 會把整個目錄上傳到 Cloud Build。
# 沒有這個檔案的話，flywheel/ 底下的**傷口影像**會被上傳、進入建置紀錄、
# 並烤進容器映像 —— 影像從此存在於一個沒有保存期限、沒有撤回機制、
# 也不在任何同意書涵蓋範圍內的地方。這是最容易發生也最難補救的外洩路徑。
#
# ⚠ 有 .gcloudignore 時 gcloud **不會**再讀 .gitignore，所以兩邊都要維護。

# ── 病人相關執行期資料（絕對不可上傳）────────────────────────────
flywheel/
uploads/
processed/
*.db
*.sqlite
*.sqlite3

# ── 日誌（可能含影像雜湊與操作軌跡）──────────────────────────
logs/
*.log

# ── 建置無關 ────────────────────────────────────────────────
.git/
.gitignore
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
.env
.DS_Store

# ── 開發用文件 ──────────────────────────────────────────────
nginx.conf
tools/

# models/ **不要**排除 —— 推論需要它們。
# 但請確認 models/ 底下只有模型權重，沒有訓練資料或病人影像。
