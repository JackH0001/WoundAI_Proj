# ===== WoundAI 協作 repo .gitignore — IP / 機敏 / 資料 防呆 =====
# 核心 IP：模型與權重（絕不進協作 repo）
*.onnx
*.h5
*.pth
*.pt
*.mlmodel
*.mlpackage
*.tflite
*.ckpt
*.pb
*.caffemodel
*.npz
*.pkl
weights/
**/saved_models/
**/checkpoints/
restored_onedrive*/
# 訓練 / 評測產出（IP）
批次驗證工具/Outputs/
批次驗證工具/Output_*/
批次驗證工具/pth/
批次驗證工具/retrain*/
**/eval_report.csv
# 資料集 / 真實傷口影像（可能個資，絕不外流）
test_images/
**/datasets/
**/data/raw/
*.dcm
# 機敏
.env
.env.*
*.pem
*.key
*.keystore
*.jks
secrets.*
**/local.properties
# 例外：允許「stub 假模型」進協作 repo（供建置/測試）
!**/stub/*.onnx
!**/*_stub.onnx

# python cache
__pycache__/
*.pyc

# phase1 ui generated
engineering/phase1/ui/expected.json
node_modules/

# phase2 runtime queue (不進 repo)
engineering/phase2/*.jsonl

**/build/

# runtime/IDE artifacts (cleanup 2026-07-07)
Android/.gradle/
**/.idea/
# ⚠ 資料庫與日誌一律排除，**不限目錄**。
#
# 2026-08-06：`wound_analysis.db`、`wound_analysis.db-journal`、`logs/wound_analysis.log`
# 被提交進這個公開 repo。這次三個都是 0 bytes（沒有資料外洩），但原因值得記下來：
#
# 舊規則寫成 `Backend/Flask/*.db`，只擋得住「從 Backend/Flask 啟動」的那一種情況。
# 而 app.py 是在**當前工作目錄**建 SQLite 與 logs/ 的——從 repo 根目錄跑一次測試
# （或任何人從別的目錄跑一次 app.py），檔案就落在規則涵蓋不到的地方，
# 而 `git add -A` 不會問你任何問題。
#
# 這個 repo 裡沒有任何一個 .db/.log 是該進版控的，所以規則就寫成無條件排除。
# 真的有例外時再用 `git add -f`——那時至少是一個有意識的決定。
*.db
*.db-journal
*.sqlite
*.sqlite3
*.log
logs/
Backend/Flask/logs/
# 飛輪執行期資料:醫師標註佇列、稽核軌跡、**病人傷口原始影像**、匯出的訓練集。
# 2026-07-28:原本只擋 *.jsonl,但 classify 現在會把上傳影像存進 flywheel/images/,
# 舊規則會讓病人照片被提交進「公開協作 repo」→ 改為整個目錄排除,只保留 README。
# 註:用 `flywheel/*` 而非 `flywheel/`——排除「目錄本身」時 git 不會再看裡面的 ! 例外規則,
# README 會一起被擋掉。排除「目錄內容」才能把 README 放行。
Backend/Flask/flywheel/*
!Backend/Flask/flywheel/README.md
# /api/train 走的是另一條路徑,同樣會落地病人影像與醫師遮罩(save_training_image/mask),
# app.py 啟動時還會在 CWD 自動建這些目錄 → 一併排除,否則飛輪堵住了這裡照樣外洩。
Backend/Flask/uploads/
Backend/Flask/processed/
uploads/
processed/

# 部署時從 engineering/ 複製的模組副本（Docker 建置上下文只有 Backend/Flask）。
# 不進版控：同一份程式碼放兩個地方，遲早有人只改了其中一個。
Backend/Flask/vendor/

# 發布簽章與產出的 APK。金鑰外流＝別人可以發佈「看起來是你們的」更新。
Android/keystore.properties
Android/_dist/

# 開通帳號時產生的明文密碼清單；以及帳號檔（含 PBKDF2 雜湊，仍屬敏感）。
Backend/Flask/accounts_*.csv
Backend/Flask/flywheel/users.jsonl

# 從手機拉下來的螢幕截圖。
#
# 這一條的理由與其他排除項不同：截圖**看起來只是 UI 畫面**，所以最容易被順手加進版控。
# 但傷口量測 App 的截圖幾乎必然含傷口影像，個案畫面還會含病患姓名與病歷號——
# 那正是整個架構最努力不讓它離開手機的兩樣東西。
# 一旦推上公開 repo，git 歷史裡就永遠拿得回來，改 .gitignore 也救不了。
# 要在 issue 或文件裡放畫面，請先手動去識別，再明確地 `git add -f` 那一張。
_screenshots/
screenshots/
Screenshots/

# XcodeGen 產出的專案檔。單一事實來源是 `iOS/project.yml`——把產物一起版控，
# 就會出現「有人改了 pbxproj 但沒改 project.yml，下一次 generate 靜默蓋掉」的情形，
# 而那正是這個 repo 改用 XcodeGen 要解決的問題（舊的手工 pbxproj 漏收了 Pipeline/ 的 8 個檔案）。
iOS/*.xcodeproj/
**/xcuserdata/
**/*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
iOS/DerivedData/

# 多行中文 commit 訊息的暫存檔。
#
# PowerShell 的 `git commit -m "..."` 對中文多行訊息會出事：`$` 會被展開、
# 反引號被當跳脫字元、`\"` 不是合法跳脫。2026-08-05 就因此吃過一次 parse error。
# 固定作法是把訊息寫進這個檔再 `git commit -F COMMIT_MSG.txt`。
# 它是過程產物，不該進版控（訊息本身已經在 git log 裡了）。
/COMMIT_MSG.txt

.DS_Store
